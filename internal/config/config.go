package config

// Struct + env/yaml loading, defaults, validation
// This file defines configuration structures and implements loading from
// environment variables and YAML files with defaults and validation
